# Business Feature Map — placeholder

> **Run `project-context` mode `features` (`/project-context features` on Claude Code) to populate or refresh this file.**

A feature-centric complement to `business-data-map.md`: feature catalog with stable `FEAT-NNN` IDs, CRUD matrix, third-party integrations, feature flags, QA relevance tags per feature.

- **Soft input for**: `project-context` modes `test-plan` and `api`.
- **Consumed by**: the `project-discovery` and `sprint-testing` skills for ATP planning and risk scoping.

That mode replaces this placeholder with the full structure (feature catalog, CRUD matrix, integrations, feature flags, QA relevance matrix, discovery gaps). Keep this file checked in as the expected anchor.

See `.agents/skills/project-context/references/features.md` for the exact output contract.
