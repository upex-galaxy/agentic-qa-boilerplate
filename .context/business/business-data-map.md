# Business Data Map — placeholder

> **Run `project-context` mode `data` (`/project-context data` on Claude Code) to populate or refresh this file.**

This file is the canonical "what this system does" map — entities, flows, state machines, triggers, cron jobs, webhooks, integrations, business rules.

- **Hard requirement for**: `project-context` mode `test-plan`.
- **Soft input for**: `project-context` modes `features` and `api`.
- **Consumed by**: the `project-discovery`, `sprint-testing`, and `test-automation` skills.

That mode replaces this placeholder with the full structure (Executive Summary, Entity Map, Flows, State Machines, Automatic Processes, Integrations, Business Rules, Discovery Gaps). Until then, keep this file checked in so both the AI and your team can see that it is the expected anchor.

See `.agents/skills/project-context/references/data.md` for the exact output contract.
